import { defineMessages } from 'react-intl'

const messages = {
  loading: {
    defaultMessage: 'Loading ...',
    id: 'transaction.tabel.placeholder.loading'
  }
}

const definedMessages = defineMessages(messages)

export default definedMessages
